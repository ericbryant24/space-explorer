{
  "name": "space-explorer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "An infinite, gentle space sandbox for small explorers.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
